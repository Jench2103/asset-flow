interface:
  display_name: "Commit Staged Changes"
  short_description: "Create a conventional commit from staged changes"
  default_prompt: "Use $commit to commit the staged changes with a concise conventional commit message."
